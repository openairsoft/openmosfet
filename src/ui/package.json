{
  "name": "ui",
  "version": "1.0.0",
  "description": "ui for OpenMosfet",
  "main": "index.js",
  "scripts": {
    "build": "mkdir build && html-inline src/index.html | html-minifier > build/out.html && node compress.js",
    "build-dev": "mkdir build && html-inline src/index.html > build/out.html && node compress.js",
    "serve-dev": "node ./dev/api/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@gfx/zopfli": "^1.0.15",
    "html-inline": "^1.2.0",
    "html-minifier": "^4.0.0"
  },
  "devDependencies": {
    "express": "4.17.1"
  }
}
